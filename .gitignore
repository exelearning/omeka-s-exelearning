.aider*
.DS_Store
/vendor/
*.zip
# PHPUnit cache files
test/.phpunit.result.cache
test/.phpunit.cache/

# Coverage reports written by `make test-coverage`
/artifacts/
themes/

# Built static editor - download from releases or build with `make build-editor`
dist/
asset/static

# Local editor checkout fetched during build (anchored to repo root so it
# does not also ignore view/exelearning/ or similar subtrees).
/exelearning/
.env
.playwright-mcp/

# Node / Playwright e2e
node_modules/
test-results/
playwright-report/
